描述：
1.查询企业端提交的购销合同
2.购销合同列表里的“状态”字段值赋值如下：
在列表接口返回值里有个“current”字段值，然后拿着每条记录里的expireTime跟“current”做对比
 if (listBean.getExpireTime() < current) {
                   无效
                } else {
                   有效
                }
------------------------------------------------------------------------------
接口：
1.列表接口：http://whp.techpolaris.com/yzb/contract/listContract
------------------------------------------------------------------------------
修改：
